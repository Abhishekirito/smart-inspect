# Environment Variables Template
# Copy this file to .env and fill in your API keys.
# For Vercel production: set these in Vercel Dashboard → Settings → Environment Variables.

# Google Cloud Vision API Key (for OCR & bounding box extraction)
VITE_GOOGLE_VISION_API_KEY=your_google_vision_api_key_here

# Google Gemini API Key (primary LLM for JSON structuring)
VITE_GEMINI_API_KEY=your_gemini_api_key_here
VITE_GEMINI_MODEL=gemini-3.6-flash

# Groq API Key & Model (auto-fallback LLM if Gemini fails)
VITE_GROQ_API_KEY=your_groq_api_key_here
VITE_GROQ_MODEL=groq/compound

# OCR.space API Key (optional fallback OCR engine — demo key works out of the box)
VITE_OCR_SPACE_API_KEY=helloworld

# Supabase (authentication + database) — REQUIRED for login and cloud storage.
# Find these in your Supabase dashboard under Project Settings → API.
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_public_key_here
